+++
title = "CHAPTER 4"
weight = 17
+++

---

### 4. 수치적 실험 및 검증

알고리즘의 대수적 제어 성능과 수치적 이산화 차수를 평가하기 위해, 본 장에서는 제조해 기법(Method of Manufactured Solutions, MMS)을 활용하여 방정식 우변의 강제 소스항 $Q(x)$를 역산하여 도출한다. 해석적 정해가 존재하지 않거나 매질 불연속성이 뚜렷한 편미분 방정식의 수치 검증에 주로 도입되는 MMS는, 이산화 오차의 누적 및 구현상의 논리적 결함을 독립적으로 식별하는 유효한 검증 수단으로 작용한다. 본 연구는 이를 통해 대수적 정화기가 작동하는 조건에서도 $\mathcal{O}(\Delta x^2)$의 이론적 점근 수렴성이 일관되게 달성됨을 정량적으로 확인한다.

최근 수치해석 분야에서는 JAX나 PyTorch와 같은 자동 미분(automatic differentiation) 프레임워크를 활용하여 연산 효율과 구현 편의를 도모하는 사례가 활용되고 있다. 그러나 본 연구에서 제안하는 대수적 보정 프레임워크는 비선형 연산자의 조립 단계부터 반대칭 성분 추출에 이르는 명시적인 대수적 제어(algebraic control)를 포함한다. 이에 따라 알고리즘의 제어 계층(control layer)과 선형대수 연산 계층(linear algebra layer)을 분리하여 64-bit double precision 환경에서 구현하였으며, 범용 자동 미분 라이브러리나 상용 PDE 패키지는 배제하였다. 프레임워크를 구성하는 세부 알고리즘 및 기하-대수적 제어 절차는 다음과 같다.

격자 이동 제어 단계에서는 3.1절에서 도출된 이산 공간 국소 비대칭 지표 $S_{kj}$를 기반으로 $r$-적응형(r-adaptive) 격자 이동을 수행한다. 오차가 발생하는 구역의 기하학적 특성을 가중치 행렬 $\hat{W}$에 반영하고, 이를 바탕으로 그래프 라플라시안(Graph Laplacian) 시스템을 구성하여 조화 사상(harmonic mapping) 방정식을 산출한다. 이 과정에서 계면 인접 노드의 자동 스내핑(auto-snapping)을 적용하여, 물성 계면이 단일 요소(element)를 관통할 때 발생하는 조화 평균 역설(harmonic mean paradox) 및 대수 왜곡을 방지한다. 이산 노드가 물리적 계면으로부터 요소 길이의 절반 미만 거리로 접근할 경우 해당 노드의 좌표를 계면 위치로 조정한다. 이를 통해 요소 내부의 킹크(kink) 발생을 억제하고, 초기 격자망이 계면과 우연히 일치하여 오차가 상쇄되는 현상(accidental error cancellation)을 통제한다.

격자 재배치 과정에서 발생하는 기하학적 변형에 의한 잔여항(residual) 증가를 제어하기 위해, 최적 상태 복원(optimal state rollback) 기법을 적용하였다. 매 반복 단계에서 비선형 잔여항의 노름(norm)을 계산하고, 최소 오차를 나타내는 기하 좌표와 상태 변수를 저장한다. 격자 이동 후 오차가 증가할 경우 시스템을 이전 상태로 복구하고 격자의 위상 변형을 동결(mesh freezing)하여 점근적 수렴성을 유도한다. 비선형 잔차($R$) 및 프레셰 도함수(Fréchet derivative, $\hat{J}$)의 조립은 해석적(analytical) 미분 법칙에 기반한다. 공간 적분은 1차원 도메인에서 다점 가우스-르장드르 구적법(multi-point Gauss-Legendre quadrature)을, 2차원 비구조적 격자계에서는 간선 중점 평가(edge-midpoint evaluation)를 적용하였다.

대수적 정규화 단계에서는 3.2절의 수반 일관성(adjoint consistency) 조건을 만족하도록 내부 레지듀를 최소화하는 대각 정규화 행렬 $\hat{C}$를 최소자승법(least squares)으로 산출한다. 또한 디리클레 경계 조건이 내부 노드의 연산자 구조에 미치는 영향을 통제하기 위해, 사영 연산자(projection operator) 형태의 마스크 행렬 $\hat{M}_s$를 적용한다. 이를 바탕으로 2.4절의 벌크-경계 소산 정리를 이용해 시스템의 비대칭성으로부터 내부 수치적 반대칭 성분($\hat{A}_{int}$)을 분리하고, 이를 원시 자코비안에서 감산하여 보정된 자코비안($\hat{J}_{pure}$)을 구성한다. 선형 시스템의 연산에는 CSC(Compressed Sparse Column) 희소 행렬 포맷을 활용하였으며, 선형 연립방정식($\hat{J}_{pure} \delta u = -R$)의 풀이에는 UMFPACK 기반 직접 희소 해법(direct sparse solver)을 적용하였다. 자코비안의 조건수(condition number) 산출에는 1-노름 기반 조건수 추정 기법을 이용하였다.

---

### 4.1 1차원 특이 섭동 시스템의 대수적 무결성 검증

무차원 유클리드 1차원 도메인 $\Omega = [0, 1]$ 상에서, 상태 종속적 비선형 확산 계수 $P(u, x)$를 가지는 강타원형(strongly elliptic) 보존 방정식은 다음과 같이 정의된다.

$$
\frac{d}{dx} \left( P(u, x) \frac{du}{dx} \right) = Q(x) \quad \text{in } \Omega
$$

시스템의 수치적 강성(stiffness)을 평가하기 위해, 도메인을 계면 $x_{int} = 0.2$ 위치를 기준으로 좌측 매질과 우측 매질로 분할하며, 확산 계수 $P(u, x)$에 불연속성을 부여한다. 우측 매질에는 투과율 단차 계수 $\sigma$가 적용된다.

$$
P(u, x) = 
\begin{cases} 
1 + u^2 & \text{if } x \le x_{int} \\
\sigma (1 + u^2) & \text{if } x > x_{int} 
\end{cases}
$$

해석적 정해 $u_{exact}(x)$는 각속도 $k = 2\pi f$를 가지는 정현파(sinusoidal wave)를 기저 함수로 하여 다음과 같이 조립된다.

$$
u_{exact}(x) = 
\begin{cases} 
\sin(k x) + C_L x & \text{if } x \le x_{int} \\
\sin(k x) + C_R (x - 1) + U_{end} & \text{if } x > x_{int} 
\end{cases}
$$

여기서 1차 계수 $C_L$과 $C_R$은 계면 $x_{int}$에서의 전위 연속성($u_L = u_R$)과 비선형 유량 연속성($P_L u_L' = P_R u_R'$)을 만족하도록 역산된 고정 상수이며, 우측 종단($x=1$)의 목표 전위는 $U_{end} = 1.0$으로 설정된다.

알고리즘의 대수적 제어 능력을 검증하기 위해 두 가지 수치적 제약(numerical stressor)을 통제 변수로 고정한다. 첫째, 매질의 계면 위치를 도메인 중앙이 아닌 $x_{int} = 0.2$에 배치하여, 균일 격자 분할 시 계면이 특정 노드와 일치함으로써 보간 오차가 소거되는 기하학적 우연성(lucky alignment)을 배제한다. 둘째, 도메인 우측 종단($x=1$)에 비대칭 로빈 혼합 경계 조건($\alpha=2.0, \beta=1.0$, $\alpha u + \beta J = \gamma$)을 적용한다. 이는 자코비안 행렬 구축 시 대각 지배성(diagonal dominance)을 약화시키고 대수적 비대칭성을 유도한다.

**1) Case A: 공간 분해능 확장에 따른 점근적 수렴성 증명 (Varying $N$)**

첫 번째 시나리오는 통제 조건($\sigma=10^{-3}, f=10.0$) 하에서 공간 분해능 $N$을 $32$에서 $4096$까지 배수로 증가시키며, 알고리즘의 거시적 수렴 특성을 평가한다.

[Figure 4.1 위치: $N=64$ 해상도에서의 해석적 정해(Exact) 및 수치해(Numerical) 중첩 프로파일 도식 삽입]

[Figure 4.2 위치: $N$ 변화에 따른 $L_2$ 및 $L_\infty$ 오차의 양축 로그 스케일(Log-Log) 수렴 궤적 삽입]

[Table 4.1 위치: 격자 해상도 $N$ 증가에 따른 수렴 및 대수 지표 통합 표 삽입]

그림 4.1은 조립 해법(MMS)에 의해 구성된 정해의 물리적 킹크(kink) 특성과 수치해의 정합성을 나타낸다. 표 4.1과 그림 4.2의 스펙트럼 궤적을 분석하면, 격자 해상도 $N$ 이 증가하는 전 구간에 걸쳐 $L_2$ 및 $L_\infty$ 오차 노름은 조대 격자(coarse mesh)로부터 조밀 격자까지 일정한 기울기(-2)를 유지하며 선형적으로 하강하였다. 주목할 점은 $N=4096$ 구간에서 파동의 고해상도 이산화에 의해 원시 자코비안(Raw Jacobian)의 조건수가 $1.20 \times 10^{13}$ 이라는 악조건(ill-conditioned) 상태에 도달했다는 점이다. 일반적으로 이러한 조건수 대역에서는 반올림 오차(round-off error)의 과도 성장에 의해 수렴 정체(stagnation)가 발생한다. 그러나 내부 소산을 분리하는 대수적 정화 과정이 탐색 행렬의 비정규성을 통제함으로써, 수치 오차의 반등 현상 없이 이론적인 $\mathcal{O}(\Delta x^2)$ 의 2차 점근적 수렴성이 일관되게 보존됨을 확인하였다.

**2) Case B: 매질 불연속성 변화에 따른 자코비안 대수적 방어능력 검증 (Varying $\sigma$)**

두 번째 시나리오는 파동 주파수($f=10.0$)와 격자 수($N=256$)를 고정한 상태에서 투과율 단차 계수 $\sigma$를 $10^{-1}$에서 $10^{-4}$까지 감소시키며, 프레셰 도함수에 유발되는 대수적 왜곡과 이에 대한 정화기(purifier)의 안정화 효과를 측정한다.

[Table 4.2 위치: $\sigma$ 축소 변화에 따른 대수적 강건성 지표 표 삽입]

표 4.2의 결과를 분석하면, $\sigma$ 가 $10^{-4}$ 로 축소됨에 따라 매질 간 유효 투과율의 급격한 비대칭성으로 인해 원시 자코비안의 조건수 $Cond(J)$ 는 비례적으로 상승하여 $4.07 \times 10^9$ 에 도달하였다. 그러나 대수적 정화 프레임워크를 통해 수반 정규화 행렬 $\hat{C}$ 와 사영 연산자가 적용된 결과, 보정된 자코비안의 잔류 비대칭 지표(treated asymmetry)는 기계 엡실론(machine epsilon) 대역인 $4.55 \times 10^{-13}$ 수준으로 강력하게 억제되었다. 이는 물성 단차와 비대칭 로빈 경계가 유발하는 시스템 교차 결합 성분의 스퓨리어스(spurious)한 비대칭화를 제안된 맵핑 벡터 체계가 성공적으로 상쇄하고 있음을 수리적으로 입증한다.

**3) Case C: 고주파 특이 곡률 확장에 따른 위상 적응 및 대수 제어 검증 (Varying $f$)**

세 번째 시나리오는 공간 분해능($N=256$)과 투과율 단차($\sigma=10^{-2}$)를 고정한 상태에서, 파동 주파수($f$)를 $1.0$에서 $20.0$으로 확장하며 국소적 곡률 증가가 유발하는 기하학적 텐션 및 위상 적응 과정을 분석한다.

[Figure 4.3 위치: 파동 주파수 $f$ 변화에 따른 공간적 압축 밀도 $\rho(x)$ 진화 도식 삽입]

[Table 4.3 위치: 주파수 $f$ 확장에 따른 기하/대수적 텐션 제어 표 삽입]

그림 4.3은 주파수 증가에 따른 시스템의 자율적 위상 변형 과정을 나타낸다. 파동 주파수 $f$ 가 상승하여 해의 곡률 반경이 감소할수록, 국소 텐션 모델과 결합된 r-적응형 엔진은 해당 영역으로 격자점을 능동적으로 밀집시킨다. 이 과정에서 이산 공간의 압축 밀도 $\rho$ 피크는 파동의 공간 주기에 동기화되어 변화율이 높은 구간의 해상도를 국소적으로 확보한다. $f=20.0$ 의 고주파 환경에서는 이산화 오차의 누적으로 인해 원시 자코비안 조건수가 $3.64 \times 10^{10}$ 으로 급증한다. 그러나 정화 자코비안은 추출된 대류 잔차를 감산함으로써 잔류 비대칭성을 $2.09 \times 10^{-9}$ 수준으로 통제하였다. 그 결과 조건수 안정화 배율(Stabilization factor)은 최대 409.5배로 산출되었으며, 오차 발산 없이 $\mathcal{O}(10^{-7})$ 대역의 안정화된 $L_2$ 오차 노름을 유지하였다.

---

### 4.2 2차원 특이 섭동 시스템의 대수적 무결성 검증

1차원 공간에서의 검증을 확장하여, 본 절에서는 교차 미분항(cross-derivative)이 존재하고 기하학적 자유도가 높은 2차원 비구조적 삼각 격자계(unstructured triangular mesh)를 대상으로 2장과 3장에서 도출된 대수적 정화 프레임워크의 타당성을 검증한다. 2차원 도메인 $\Omega = [0, 1] \times [0, 1]$ 상에서 정의되는 비선형 타원형 편미분 방정식은 다음과 같다.

$$
\nabla \cdot \left( P(u) \nabla u \right) = Q(x, y) \quad \text{in } \Omega
$$

수치적 강성을 제어하기 위해 지수 함수 형태의 비선형 확산 계수 $P(u) = \exp(k_{nl} u)$를 도입한다. 도메인은 수직 계면 $x_{int} = 0.7$을 기준으로 좌측 매질과 우측 매질로 분할되며, 우측 매질에는 투과율 계수 $\sigma$가 적용되어 큰 폭의 물성 불연속성을 유도한다. 조립 해법(MMS)의 해석적 정해로는 다음과 같은 지수 감쇠 모델을 적용하였다.

$$
u_{exact}(x, y) = 
\begin{cases} 
U_0 \exp(-\alpha_L x) \cos(\pi y) & \text{if } x \le x_{int} \\
U_0 \exp(-\alpha_L x_{int}) \exp(-\alpha_R (x - x_{int})) \cos(\pi y) & \text{if } x > x_{int} 
\end{cases}
$$

기준 진폭을 $U_0 = 1.0$, 좌측 매질의 감쇠율을 $\alpha_L = 2.0$으로 상정할 때, 우측 매질의 감쇠율 $\alpha_R$은 계면에서의 비선형 유량 보존 법칙($P_L \nabla u_L \cdot \mathbf{n} = P_R \nabla u_R \cdot \mathbf{n}$)에 의해 결정된다. 연속 조건($u_L = u_R$)에 의해 양변의 비선형 항이 소거됨으로써, 우측 감쇠율은 투과율 단차에 반비례하는 $\alpha_R = \alpha_L / \sigma$ 이다.

**1) Case A: 비선형 강도 스윕에 따른 프레셰 도함수 비대칭성 제어 및 대수적 정화 검증 (Varying $k_{nl}$)**

첫 번째 시나리오는 통제 조건($\sigma=10^{-2}, k_{nl}=2.0$) 하에서 공간 분해능 $N_x$를 8에서 64로 확장하며, 비정합 격자망에서 프레임워크가 점근적 수렴성을 유지하는지 확인한다.

Figure 4.4 위치: 2차원 해석적 정해(지수 감쇠 모델)의 표면(Surface) 및 등고선(Contour) 도식 삽입]

[Figure 4.5 위치: $N_x$ 증가에 따른 2D 비구조적 격자계의 Log-Log 오차 수렴 궤적 삽입]

[Table 4.4 위치: 2D 격자 해상도 $N_x$ 확장에 따른 스펙트럼 지표 표 삽입]

수치 실험 결과, $N_x=64$ 구간에서 원시 비대칭성(Raw Asymmetry) 지표가 644.0 규모로 관측되었다. 이는 방향성 편향을 내포한 2차원 비정합 삼각 격자망에서, 단순한 기하학적 보정만으로는 유효 속도장의 발산 연산 시 발생하는 이산 교환자 오차를 완전히 상쇄할 수 없음을 수리적으로 보여준다. 그러나 이산 헬름홀츠-호지 분해(DHHD)에 근거하여 직교 여공간 사영을 수행한 결과, 대수적 정화기는 잔류 비대칭성을 $2.84 \times 10^{-2}$ 수준으로 저감시켰다. 이를 통해 5.0배의 조건수 안정화 효과를 확보하였으며, $L_2$ 오차 노름 기준 $2.56 \times 10^{-3}$ 에 도달하여 2차원 비구조적 격자계에서도 이론적인 $\mathcal{O}(h^2)$ 의 점근적 수렴성이 일관되게 달성됨을 입증하였다.

**2) Case B: 이산화 한계 공간에서의 대수적 생존성 평가 (Varying $\sigma$)**

두 번째 시나리오는 $k_{nl}=1.0$, $N_x=32$ 조건 하에서 투과율 단차 $\sigma$를 $1.0$에서 $10^{-3}$까지 점진적으로 축소시킨다. 해당 실험은 계면의 미분 불연속성이 격자의 기하학적 해상도 한계를 초과했을 때 유발되는 기하학적 위상 변형과 시스템의 대수적 안정성을 분석한다.

[Figure 4.6 위치: 투과율 단차 $\sigma$ 축소에 따른 2D 비구조적 격자망의 국소 밀집(Mesh Deformation) 도식 삽입]

[Table 4.5 위치: 투과율 단차 $\sigma$ 축소에 따른 극한 이산화 한계 생존성 지표 표 삽입]

$\sigma=10^{-3}$ 인 경우 우측 매질의 감쇠율은 $\alpha_R=2000$ 수준으로 급증하며, $x_{int}=0.7$ 계면에서 극단적인 미분 불연속성(kink)을 형성한다. 이를 분해하기 위해 r-적응형 모듈은 비구조적 격자 연결망을 해당 계면으로 강하게 밀집시킨다. 위상 적응에도 불구하고 이산 공간의 근본적 해상도 부족으로 인해 절단 오차(truncation error)가 잔존하며 $Cond(J) \approx 10^6$ 수준의 악조건이 유발된다. 그 결과 물리적 $L_2$ 오차는 $8.88 \times 10^{-2}$, 최대 오차는 $3.59 \times 10^{-1}$ 로 상승한다. 그러나 대수적으로 정화된 자코비안의 잔류 비대칭성은 여전히 $2.30 \times 10^{-3}$ 대역으로 억제되었다. 이는 공간적 해상도가 부족하여 정확한 진해 추적에 한계가 있는 상황에서도, 대수적 정화 엔진이 해당 이산 공간이 수용 가능한 갤러킨 투영(Galerkin projection) 기반의 평형 상태를 발산 없이 안정적으로 도출함을 나타낸다.

**3) Case C: 비선형 강도 스윕에 따른 비대칭 제어 (Varying $k_{nl}$)**

마지막 시나리오는 $N_x=32$, $\sigma=10^{-1}$ 상태에서 비선형 강도 지수 $k_{nl}$을 $0.0$에서 $4.0$까지 변화시키며, 프레셰 도함수의 비대칭성이 대수적 구조에 미치는 영향을 추적한다.

[Figure 4.6 위치: 비선형 강도 $k_{nl}$ 증가에 따른 2D 비구조적 격자망의 국소 밀집(Mesh Deformation) 도식 삽입]

[Table 4.6 위치: 비선형 강도 $k_{nl}$ 변화에 따른 2차원 대수적 강건성 지표 표 삽입]

지수형 비선형 구배($\partial P/\partial u$)가 증폭됨에 따라 시스템 텐서의 교차 대칭성 상실 속도가 가속된다. $k_{nl}=4.0$ 조건에서 원시 자코비안의 비대칭성 지표는 3,304 규모로 폭증한다. 그러나 반대칭 성분의 대수적 분리 및 감산 절차를 거친 후, 정화된 시스템의 잔류 비대칭성은 $6.28 \times 10^{-1}$ 수준으로 억제되었다. 특히 이 조건에서 달성된 조건수 안정화 비율은 최대 979.1배로 산출되었다. 이는 외부의 추가적인 기하학적 통제나 인위적인 수치 소산 부과 없이, 본 연구에서 제안한 순수 대수적 정화 프레임워크만으로도 $2.57 \times 10^{-3}$ 대역의 안정된 $L_2$ 오차 노름을 일관되게 유지하며 고강도 비선형 시스템을 제어할 수 있음을 종합적으로 입증한다.