+++
title = "전체 그림"
weight = 1
+++

---

# 00. 전체 그림

이 문서는 **지도**입니다.  무슨 문제를 푸는지, 어떤 조각으로 이루어졌는지,
그 조각이 어느 파일인지를 한자리에 모았습니다.

자세한 것은 아래 문서로 내려갑니다.

| 층 | 문서 | 무엇 |
|---|---|---|
| 사양 | `10_격자와물성.md` | 영역을 정의하고 물성을 컴파일하고 격자를 만든다 |
| 사양 | `20_조립.md` | 잔차와 자코비안을 만들고 순화한다 |
| 사양 | `30_뉴턴풀이.md` | 비선형 방정식을 푼다 |
| 사양 | `40_격자적응.md` | 격자를 옮기고 늘린다 |
| 사양 | `50_시간전진.md` | 시간을 전진시킨다 |
| 유도 | `60_유도_상한.md` | 왜 그 상한식인가 |
| 유도 | `70_유도_장치.md` | 왜 그 감쇠·보폭·배수식인가 |
| 기록 | `legacy/` | 어떻게 알아냈나.  실측과 폐기 기록 |

---

## 1. 무슨 문제를 푸는가

정상 상태의 준선형 타원형 방정식입니다.

$$\nabla\cdot\bigl(p(u,x,y)\,\nabla u\bigr) + q(u,x,y) = 0$$

| 기호 | 뜻 | 예 |
|---|---|---|
| $u$ | 구하려는 값 | 온도, 전위 |
| $p$ | 확산 계수 | 열전도율, 유전율 |
| $q$ | 소스 | 발열, 전하 |

**$q$ 는 소스 그 자체입니다.**  발열이면 양수를 그대로 넣습니다.
열전도에서 흔히 쓰는 표기와 같습니다.

$$\nabla\cdot(\kappa\nabla T) + Q = 0 \qquad (Q > 0 \text{ 이 발열})$$

> 게재된 논문은 $\nabla\cdot(p\nabla u) = q$ 로 잡아 $q$ 의 부호가 반대입니다.
> 그쪽 식을 함께 볼 때는 $q$ 를 뒤집어 읽어야 합니다.

**두 가지가 어렵게 만듭니다.**

| | 무엇 | 왜 어려운가 |
|---|---|---|
| 1 | $p$ 가 $u$ 에 의존한다 (준선형) | 자코비안이 비대칭이 된다 |
| 2 | $p$ 가 물질 경계에서 불연속이다 | 계면에서 계수가 몇 자릿수 뛴다 |

$p = 148\,u^{-1.3}$ 인 실리콘과 $p = 1.4$ 인 산화막이 맞닿는 FinFET 자기발열이
그 예입니다.  온도가 오르면 열전도가 떨어져 발열이 더 심해집니다.

**시간 항이 필요할 때도 있습니다.**

$$c(x,y)\,\frac{\partial u}{\partial t} = \nabla\cdot(p\nabla u) + q$$

정상해가 아예 없는 구성이 있습니다.  소스가 임계를 넘으면 열폭주가 일어나
정상 문제로는 못 풉니다.  그 경우 시간을 전진시켜야 합니다.

---

## 2. 기호표 — 한 글자에 한 뜻

**성질이 다르면 다른 글자를 씁니다.**  성질이 같은 무리이면 첨자로 가릅니다.
서체나 대소문자로 가르지 않습니다.  손으로 쓸 때 안 보이기 때문입니다.

### 문제를 세우는 것

| 기호 | 뜻 |
|---|---|
| $u$ | 구하려는 값 |
| $p$ | 확산 계수 |
| $q$ | 소스.  **부호를 안 뒤집는다.**  발열이면 양수 |
| $c$ | 시간 항 계수 |
| $\alpha,\ \beta,\ \gamma$ | Robin 경계조건의 계수 |
| $T,\ Q,\ k$ | 열전도 예에서만.  온도, 발열, 열전도율 |

### 격자

| 기호 | 뜻 |
|---|---|
| $\tilde V_i$ | 노드 $i$ 가 맡은 넓이 |
| $D_{ij}$ | 간선 $ij$ 의 가중치 |
| $\theta_1,\ \theta_2$ | 간선을 마주 보는 두 각 |
| $h$ | 간선 길이 |
| $\ell_x,\ \ell_y$ | 영역의 가로세로 크기 |
| $\ell_{\text D}$ | 디바이 길이 |
| $N_x,\ N_y$ | 각 방향 분할 수 |
| $\mathbf E$ | 간선-노드 기울기 연산자 |
| $\mathcal L$ | r-적응이 푸는 라플라스 연산자 |
| $\omega_i$ | r-적응에서 노드가 당기는 힘 |
| $\gamma_h$ | 격자를 늘릴 때 $\nu_{\max}$ 가 줄어드는 배수 |

### 행렬

| 기호 | 뜻 |
|---|---|
| $\hat J$ | 자코비안 |
| $\hat C$ | 노드마다 곱한 배율.  대각 |
| $\hat c_i$ | 그 대각 성분 |
| $S$ | 대칭부 |
| $A$ | 반대칭부 |
| $B$ | 좌표를 바꾼 반대칭 행렬 |
| $\chi_{ij}$ | $-S_{ij}$.  간선이 얼마나 잘 통하는가 |
| $\gamma_i$ | $S$ 의 행 합 |
| $\varepsilon_{ij}$ | $A_{ij}/S_{ij}$ |
| $\Phi_{ii}$ | $\sum_j \varepsilon_{ij}^2\chi_{ij}$ |
| $M$ | 시간 항 질량.  $\mathrm{diag}(c_i\tilde V_i)$ |
| $\Pi$ | Dirichlet 행과 열을 지우는 사영 행렬 |
| $R$ | 촐레스키 인자.  $S = R^{\mathsf T}R$ |
| $\kappa$ | 조건수 |
| $\lambda$ | 고윳값 |
| $\eta$ | 비대칭이 흡수된 비율 |
| $J_+,\ J_-$ | 간선 양쪽에서 본 자코비안 성분의 크기 |

### 푸는 것

| 기호 | 뜻 |
|---|---|
| $\rho_{\mathrm{int}}$ | 반대칭부의 크기 |
| $\mathbf w$ | 시험 벡터 |
| $w_i,\ w_j$ | 그 성분 |
| $\delta_{ij}$ | 시험 벡터의 간선 양끝 차 |
| $\theta_{ij}$ | 시험 벡터의 간선 양끝 위상차 |
| $\delta\mathbf u$ | 한 걸음 |
| $\delta t$ | 시간 간격 |
| $\delta t_{\max}$ | 그 상한 |
| $\delta k$ | 반복 횟수가 늘어난 정도 |
| $\sigma$ | 감쇠 인자 |
| $\varsigma(\sigma)$ | 잔차 축소율 |
| $e_\lambda,\ e_\varsigma$ | 고윳값과 축소율의 오차 |
| $\nu_j$ | $(\partial\ln p/\partial u)\vert_j\cdot\delta u$ |
| $\nu_{\max}$ | $\lvert\nu\rvert$ 의 최대값 |
| $\zeta_{ij}$ | $\ln(p_j/p_i)$.  간선 양끝의 물성 비 |
| $\xi$ | 상한 C 를 훑을 때의 매개변수 |
| $\sigma_{\text{부호}}$ | 비대각의 부호가 안 뒤집히는 최대 걸음 |

---

### 첨자로 갈린 무리

| 무리 | 갈래 |
|---|---|
| $\gamma$ | Robin 계수 / $\gamma_i$ 행 합 / $\gamma_h$ 격자 배수 |
| $\delta$ | $\delta\mathbf u$ 걸음 / $\delta t$ 시간 / $\delta k$ 반복 / $\delta_{ij}$ 간선 차 |
| $\theta$ | $\theta_1,\theta_2$ 삼각형 각 / $\theta_{ij}$ 위상차 |
| $\nu$ | $\nu_j$ 노드에서 / $\nu_{\max}$ 최대 |
| $e$ | $e_\lambda$ 고윳값 오차 / $e_\varsigma$ 축소율 오차 |

$$\text{첨자로 가른 것은 \textbf{성질이 같은 무리}입니다}$$

$\gamma$ 와 $\gamma_i$ 는 둘 다 계수이고, $\delta$ 무리는 모두 「얼마만큼의 차이」입니다.
반면 흡수 비율 $\eta$ 와 축소율 $\varsigma$ 는 둘 다 비율이지만 잰 대상이 아예 달라
다른 글자를 씁니다.

### 안 쓰는 글자

$\iota$, $\upsilon$, $\pi$, $\mu$, $\phi$, $\psi$, $\Lambda$, $\Sigma$, $\Theta$, $\Omega$, $\Upsilon$

$i$ 나 $v$ 와 헷갈리거나, 다른 뜻으로 이미 굳어 있는 글자입니다.

---


## 3. 왜 전처리가 필요한가

유한체적법으로 이산화하면 자코비안 $\hat J$ 가 나옵니다.  **이것이 비대칭입니다.**

$$\hat J_{ij} \ne \hat J_{ji}$$

비대칭이면 켤레기울기법을 못 쓰고, GMRES 를 써도 수렴이 느립니다.

### 대칭·반대칭으로 가릅니다

$$\hat J = \underbrace{\tfrac{1}{2}(\hat J + \hat J^{\mathsf T})}_{\text{대칭}} + \underbrace{\tfrac{1}{2}(\hat J - \hat J^{\mathsf T})}_{\text{반대칭}}$$

대칭부만 남기면 풀기 쉽습니다.  문제는 **반대칭부를 버려도 되는가** 입니다.

$$\rho_{\mathrm{int}} = \sup_{\mathbf w \ne 0} \frac{\lvert\mathbf w^{*}A\mathbf w\rvert}{\mathbf w^{*}S\mathbf w}$$

이 값이 1보다 작으면 대칭부만으로 만든 전처리기가 잘 듣습니다.
$\rho_{\mathrm{int}}$ 를 실제로 재고, 그것으로 감쇠와 허용오차를 정합니다.

> 유도는 `60_유도_상한.md`.

### 노드마다 배율을 곱해 격자 탓을 먼저 걷어냅니다

비대칭에는 두 가지가 섞여 있습니다.

| 섞인 것 | 왜 생기나 |
|---|---|
| 격자 탓 | 노드마다 맡은 넓이가 달라서 |
| 물리 탓 | $p$ 가 $u$ 에 따라 달라져서 |

앞의 것은 걷어낼 수 있습니다.  노드마다 배율 $\hat c_i$ 를 하나씩 곱하면 됩니다.

$$\hat C = \mathrm{diag}(\hat c_i)$$

그 배율은 이렇게 찾습니다.  간선마다 양쪽에서 본 두 성분의 비를 로그로 재고,
그것이 **노드마다 정해진 어떤 수의 차이**로 얼마나 설명되는지를 봅니다.
가장 잘 설명하는 값을 최소제곱으로 구하면 그것이 $\hat c$ 입니다.

**설명되지 않고 남는 것은 그대로 둡니다.**

$$\hat C\hat J = \underbrace{\tilde J_{\text{pure}}}_{\text{대칭}} + \underbrace{\tilde A_{\text{int}}}_{\text{반대칭}}$$

남은 $\tilde A_{\text{int}}$ 안에 **해가 어느 쪽으로 가야 하는지**가 들어 있습니다.
온도가 오르면 열전도가 떨어지는 물질에서 뜨거운 쪽과 찬 쪽은 서로 대칭이
아닙니다.  그 방향까지 지우면 푸는 문제가 달라집니다.

$$\text{걷어낼 수 있는 만큼만 걷어내고, 남은 것은 얼마나 큰지 재서 다룹니다}$$

---

## 4. 어떤 조각으로 이루어졌는가

```
   영역 정의          물성            격자
        │              │               │
        └──────────────┴───────────────┘
                       │
                    조 립                    <- 20_조립.md
                       │
                  AssembledSystem
                       │
        ┌──────────────┼───────────────┐
        │              │               │
     선형해         감쇠·보폭        상한·rho      <- 30_뉴턴풀이.md
        │              │               │
        └──────────────┴───────────────┘
                       │
                   뉴턴 반복
                       │
        ┌──────────────┴───────────────┐
        │                              │
    격자 적응                      시간 전진
  40_격자적응.md                  50_시간전진.md
```

**푼 뒤에 보는 것은 따로 있습니다.**

```
                  AssembledSystem
                       │
        ┌──────────────┼───────────────┐
        │              │               │
  SolutionAnalyzer  PreconditionAnalyzer  UniversalVisualizer2D
   해가 맞는가       계가 풀기 좋은가        그림
```

$$\text{셋 다 조립 결과를 \textbf{받아서} 잽니다}$$

푸는 쪽의 클래스를 안 부릅니다.  물성을 다시 평가하거나 행렬을 다시
조립하지 않습니다.  `12_후처리독립.md` 에 그 기록이 있습니다.

### 되풀이가 네 겹이다

| 겹 | 어디에 있나 | 무엇을 되풀이하나 | 몇 번까지 |
|---|---|---|---|
| 첫째 (마이크로) | 감춘 `solve` | 뉴턴 걸음을 밟는다 | `MaxMicro` |
| 둘째 (매크로) | 감춘 `solve` | 뉴턴이 끝나면 격자를 손볼지 보고 다시 뉴턴으로 | `MaxMacro` |
| 셋째 | `solveSteady` | 다 푼 해를 보고 노드 수가 모자라면 격자를 새로 만들어 다시 푼다 | `HAdaptOpt.MaxHRefine` |
| 넷째 | `solveTransient` | 한 시각을 다 풀면 다음 시각으로 | `MaxSteps` |

앞의 둘이 감춘 `solve` 안에 있습니다.  마이크로가 수렴하고 격자가 더
안 움직이면 그 안에서 끝납니다.

**셋째와 넷째는 나란한 층이고 서로를 안 부릅니다.**  `solveTransient` 은
`solveSteady` 를 거치지 않고 자기 안에서 격자를 손봅니다.  시각마다
지켜야 할 순서가 있기 때문입니다.

```
격자를 늘린다  ->  해를 옮겨 담는다  ->  시간 간격을 잰다  ->  푼다
```

**각 겹에서 무엇을 할지는 옵션이 정합니다.**

| 겹 | 옵션 | 참이면 |
|---|---|---|
| 둘째 | `sv.UseRAdapt` | 노드를 급한 자리로 옮긴다.  노드 수는 그대로다 |
| 셋째 | `sv.UseHAdapt` | 노드 수가 모자라면 늘려서 다시 푼다 |

둘 다 기본이 켬입니다.  아무것도 안 정해도 알맞은 격자를 찾습니다.

---

## 5. 어느 조각이 어느 파일인가

### 5.1 영역·물성·격자 → `10_격자와물성.md`

| 파일 | 하는 일 |
|---|---|
| `ContinuousDomain2D.m` | 영역을 등록한다.  두 영역이 겹치면 어느 쪽을 따를지 정한다 |
| `resolveRegions.m` | 점마다 어느 영역의 물성·경계조건을 쓸지 정한다 |
| `PhysicsManager2D.m` | 물성을 갖고 점마다 $p,\ \partial p/\partial u,\ q,\ \partial q/\partial u$ 를 낸다 |
| `compileEquation.m` | 수식 문자열을 빠른 함수 핸들로 바꾼다 |
| `MeshManager2D.m` | 격자를 만들고, 유한체적법이 쓰는 면적과 간선 가중치를 미리 재 둔다 |
| `RegionShapeBase.m` 외 7 | 점·선·원·타원·사각형·다각형·음함수 |
| `CoordinateSystem.m` `Cartesian2D.m` `Cylindrical2D.m` | 좌표계 |
| `MeshSnapshot.m` | 격자 상태의 스냅숏 |

### 5.2 조립 → `20_조립.md`

| 파일 | 하는 일 |
|---|---|
| `Purifier2D.m` | 잔차와 자코비안을 만들고 대칭·반대칭으로 가른다.  **부르는 곳은 `assemble2D` 하나** |
| `AdjointCExtractor.m` | 격자 때문에 생긴 비대칭을 흡수할 대각 배율 $\hat C$ 를 구한다 |
| `AssembledSystem.m` | 조립이 낸 것을 통째로 담는 그릇.  상한을 재는 셈법 셋을 함께 갖는다 |
| `assembleRobinFaces.m` | Robin 경계를 면 단위로 |
| `assembleRobinNodal.m` | 면으로 안 잡힌 나머지 노드 |
| `dirichletRowsOf.m` | 행렬 구조에서 Dirichlet 행을 가른다 |

### 5.3 뉴턴 풀이 → `30_뉴턴풀이.md`

| 파일 | 하는 일 |
|---|---|
| `@ResidueSolverNR2D/` | **주 진입점.**  파일 다섯으로 나뉘어 있다 |
| `  ResidueSolverNR2D.m` | 클래스 선언, 속성, 생성자, 안에서만 쓰는 것 |
| `  solve.m` | 한 상태를 한 번 푼다.  이중 루프를 돌린다 |
| `  solveSteady.m` | 정상 문제.  격자가 부족하면 늘려 다시 푼다 |
| `  solveTransient.m` | 시각을 밟아 간다 |
| `  setOptions.m` | 여러 값을 한 번에 준다 |
| `AdaptiveDamper.m` | $\rho$ 로 감쇠 인자를 정한다 |
| `DamperBase.m` `ClipEachDamper.m` `KeepRatioDamper.m` | 다른 감쇠 방식 |
| `trustRegionStep.m` | M-행렬 구조를 지키는 보폭 상한 |
| `adaptiveTolerances.m` | 감쇠 인자와 허용오차를 한꺼번에 |
| `LinearSolverBase.m` `DirectSolver.m` `GMRESSolver.m` | 선형해 |
| `GoldenStateManager.m` | 잔차가 가장 낮았던 순간의 해와 격자를 기억해 둔다 |
| `ConvergenceMonitor.m` | 수렴·정체·발산 판정 |
| `SolverLogger.m` | 반복마다 로그 |
| `SolutionAnalyzer.m` | 푼 해가 물리적으로 맞는지 잰다.  보존, 정해와의 차이 |
| `PreconditionAnalyzer.m` | 조립된 자코비안이 반복법으로 풀기 좋은지 잰다 |
| `assembleSnapshot.m` | 걸음마다의 기록에서 하나를 골라 조립한다 |
| `dualSeed.m` | 초기해 생성 |

### 5.4 격자 적응 → `40_격자적응.md`

| 파일 | 하는 일 |
|---|---|
| `AdjointTensionRelaxer2D.m` | 격자를 더 옮길지 여기서 멈출지 정한다 |
| `relaxMesh.m` | 노드를 실제로 옮긴다 (r-적응) |
| `meshSufficiency.m` | 격자가 충분한지 판정하고 필요한 $N_x$ 를 낸다 |
| `@ResidueSolverNR2D/solveSteady.m` | 정상 문제를 푼다.  격자가 부족하면 늘려 다시 푼다 |
| `MeshHRefiner.m` | 노드를 늘릴 때 쓸 값을 담는다.  `sv.HAdaptOpt` 다 |

### 5.5 시간 전진 → `50_시간전진.md`

| 파일 | 하는 일 |
|---|---|
| `@ResidueSolverNR2D/solveTransient.m` | 시간을 전진시킨다.  솔버의 메서드다 |
| `AssembledSystem.dtBound` | $\rho_{\mathrm{int}} < 1$ 을 보장하는 시간 간격 상한 |
| `interpToMesh.m` | 격자가 바뀔 때 상태를 옮긴다 |

### 5.6 그 밖

| 파일 | 하는 일 |
|---|---|
| `UniversalScaleManager.m` `SemiconductorScaler.m` | 단위를 없애 숫자 크기를 고른다 |
| `UniversalVisualizer2D.m` | 그림 |
| `SweepManager.m` | 값을 하나씩 바꿔 가며 되풀이해 푼다 |

---

## 6. 검사 스크립트

무엇이 깨지면 그것이 걸리는가로 다섯으로 나눴습니다.

| 순서 | 파일 | 실패하면 | 비용 |
|---|---|---|---|
| 1 | `CHECK_0_apply.m` | 파일 교체가 덜 됐다 | 수 초 |
| 2 | `CHECK_A_derive.m` | **유도가 틀렸다** | 초 |
| 3 | `CHECK_B_premise.m` | 그 전제가 이 구성에서 깨졌다 | 1~2분 |
| 4 | `CHECK_C_operate.m` | 장치가 일을 안 한다 | 10~20분 |
| 5 | `CHECK_D_measure.m` | 실제 격자에서 성질이 안 성립한다 | 수 분 |

**A 는 손으로 만든 행렬만 씁니다.**  수식이 참인지 거짓인지는 실제 문제와
상관이 없기 때문입니다.  **B 는 조립까지만** 하고 뉴턴 반복은 안 돌립니다.

**넷 다 코드가 어길 수 있는 것만 봅니다.**  값이 얼마인가는 `paper_` 가
냅니다.  둘을 섞으면 검사가 무거워지고, 같은 것을 두 곳에서 재게 됩니다.
무엇을 왜 뺐는지는 `11_검사전수조사.md` 에 있습니다.

`CHECK_C` 와 `CHECK_D` 는 첫머리 `SECTIONS` 한 줄로 골라 돌릴 수 있습니다.

### 공용 문제 설정

| 파일 | 무엇 |
|---|---|
| `buildMMS.m` | 정답을 미리 정해 놓고 거기에 맞는 소스를 거꾸로 만든 문제.  정답을 아니까 오차를 잴 수 있다 |
| `buildDevice.m` | FinFET 자기발열 |
| `buildRunaway.m` | 열폭주.  발열이 스스로를 키워 정상 상태가 아예 없는 문제 |

---

## 7. 논문 재현 스크립트

| 파일 | 논문 |
|---|---|
| `paper_MMS_CaseA_Convergence.m` | §4.2.3 수렴성 |
| `paper_MMS_CaseA_Plot.m` | §4.2 그림 |
| `paper_MMS_CaseB_Sigma.m` | §4.2.2 $\sigma$ 스윕 |
| `paper_MMS_CaseC_Knl.m` | §4.2.1 $k_{nl}$ 스윕 |
| `paper_MMS_CaseD_Frequency.m` | §4.2.4 주파수 |
| `paper_GMRES_KnlSweep.m` `paper_GMRES_SigmaSweep.m` | §6.4 |
| `paper_GMRES_LimitTest.m` | §6.4 격자 한계 |
| `paper_GMRES_PrecondCompare.m` | 전처리기 통제 실험 |
| `paper_HeatChannel_BetaSweep.m` | 실용 예제 |
| `paper_HeatDevice_SelfHeating.m` | 실용 예제 (자기발열) |
| `paper_HZO_nanowire.m` `paper_HZO_nanowire_report.m` | GAA FeFET C-V |

> **표를 다시 뽑아야 합니다.**  격자 만드는 방법이 달라졌으므로 격자가
> 달라졌고, 따라서 표의 수치도 달라집니다.

---

## 8. 가장 짧은 사용 예

```matlab
% 1. 영역
dom = ContinuousDomain2D('Bounds', [0 1 0 1], 'CoordSystem', Cartesian2D());
idL = dom.addRegion('Name', "Left",  'Type', "rectangle", 'Params', [0 0.5 0 1]);
idR = dom.addRegion('Name', "Right", 'Type', "rectangle", 'Params', [0.5 1 0 1]);

% 2. 물성과 경계조건
pm = PhysicsManager2D('Domain', dom);
pm.setEquation(idL, 'P', "1.0",          'Q', "1.0");
pm.setEquation(idR, 'P', "0.1*exp(2*u)", 'Q', "1.0");
pm.setEquation("Bottom", 'U', 0.0);                              % Dirichlet
pm.setEquation("Top", 'Alpha', "0.0", 'Beta', "1.0", 'Gamma', "0.0");  % Neumann
pm.compile();

% 3. 격자
mm = MeshManager2D('Domain', dom);
mm.generateMesh('Nx', 64, 'Ny', 64, 'InterfaceLevel', 2);
mm.updateMetrics(pm);

% 4. 풀이
sv = ResidueSolverNR2D('MeshM', mm, 'PhysM', pm);
[U, r_norm, steady_info] = sv.solveSteady();
```

위의 `solveSteady` 는 기본으로 노드를 옮기고 늘립니다.  $N_x$ 를 정해
놓고 재려면 h-정제를 끕니다.

```matlab
sv.UseHAdapt = false;
[U, r_norm] = sv.solveSteady();
```

시간을 전진시키려면 이렇게 합니다.

```matlab
[U, transient_info] = sv.solveTransient('TEnd', 10, 'DtPhys', 0.1);
```

---

## 9. 읽는 순서

| 무엇이 궁금한가 | 어디로 |
|---|---|
| 이 코드를 쓰고 싶다 | 7절 → `10_격자와물성.md` → `30_뉴턴풀이.md` |
| 클래스가 어떻게 되어 있는지 | `06_클래스목록.md` |
| 무엇이 클래스여야 하고 무엇이 함수여야 하는지 | `07_클래스판단.md` |
| 계층을 어떻게 고치기로 했는지 | `08_정한것.md` |
| 검사에 무엇이 있어야 하는지 | `11_검사전수조사.md` |
| 후처리가 왜 독립인지 | `12_후처리독립.md` |
| ** 아직 안 정한 것 ** | `13_남은설계.md` |
| 조립이 무엇을 만드는지 | `20_조립.md` |
| 왜 이 식인지 | `60_유도_상한.md` `70_유도_장치.md` |
| 왜 다른 방법을 안 썼는지 | `legacy/` |
| 코드를 고쳤는데 안 깨졌는지 | `CHECK_0_apply` → `A` → `B` → `C` → `D` |
